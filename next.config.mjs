/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost','i.dorimu.top','d.dorimu.cn'],
    },
    experimental: {
      },
};

export default nextConfig;
